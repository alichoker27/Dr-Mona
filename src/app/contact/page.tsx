import React from "react";
import ContactClient from "./ContactClient";

export const metadata = {
  title: "Contact Me | Psychology Portfolio",
  description:
    "Reach out via email, phone, or social media. Connect with me for professional psychological support and guidance.",
};

const ContactPage = () => {
  return <ContactClient />;
};

export default ContactPage;
